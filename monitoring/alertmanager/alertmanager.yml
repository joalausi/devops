global:
  resolve_timeout: 5m

route:
  receiver: discord-relay
  group_by: [alertname, instance]
  group_wait: 10s
  group_interval: 5m
  repeat_interval: 4h

receivers:
  - name: discord-relay
    webhook_configs:
      - url: http://discord-relay:8080/alertmanager
        send_resolved: true
